# URL of the congregation backend API (no trailing slash)
# Use localhost only for web development.
# The Android app can override this at runtime from the login screen.
VITE_API_URL=https://congregation-api.onrender.com
